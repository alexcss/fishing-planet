{#
 # DLC Card Component
 # Card for displaying DLC custom post type
 #}

{% from "components/image.twig" import image %}

{% if post %}
	{% set card_image = custom_image ? : post.thumbnail %}
	{% set dlc_category = post.get_primary_category %}
	{% set image_size = image_type == "wide" ? { w: 450, h: 254 } : { w: 413, h: 434 } %}

	<article class="group relative space-y-24 xl:space-y-40">
		{% if card_image %}
			<a href="{{ post.link }}" class="relative block overflow-hidden">
				{{ image({ image: card_image, class: "w-full transition-transform duration-500 group-hover:scale-102", sizes: image_size }) }}
			</a>
		{% endif %}

		<div class="space-y-20">
			{% if dlc_category %}
				<div class="fp-badge">
					{{ dlc_category.name }}
				</div>
			{% endif %}
			<h3 class="h4"><a href="{{ post.link }}" class="fp-link fp-stretched-link">{{ post.title }}</a></h3>
			{% if post.meta("short_description") %}
				<p class="fp-text-body-small">
					{{ post.meta("short_description") }}
				</p>
			{% endif %}
		</div>
	</article>
{% endif %}
